# Supabase — set these as Netlify environment variables (Site settings → Environment variables).
# The anon key is public by design (RLS restricts it to inserts) but must never be committed.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
